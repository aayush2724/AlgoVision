# Minimum versions (not exact pins) so pip installs prebuilt wheels for your
# Python version instead of compiling old pydantic-core from source.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.11
python-dotenv>=1.0
groq>=0.9
